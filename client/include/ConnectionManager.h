#ifndef CONNECTION_MANAGER_H
#define CONNECTION_MANAGER_H


// TODO: this class "owns" multiple c2 connections
// it gives the client a way to connect to multiple servers at once
// 

class ConnectionManager
{
public:
	//ConnectionManager();
	//~ConnectionManager();

private:
	//MessageHandler* handler;
	//std::vector<std::unique_ptr<Transporter>> transporters;
	//std::vector<ServerConfig> server_configs;
};

#endif